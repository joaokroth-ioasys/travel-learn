 Check somethings that exists on germany journey but not on d2 journey.
  Ex.:
  - labels of the countries around names
  - sequence (starts in frankfurt, goes to stuttgart, ...)

  Plan then implement

The user wants DE 2 to match the German journey's map features that it currently lacks — the two named examples (surrounding country labels, and the numbered Frankfurt→Stuttgart→… sequence), plus anything else present in de but missing in d2. Let me compare the two maps' full definitions and how MapScreen renders these features.

  Read 1 file, ran 1 shell command

The picture just changed again: the reminder shows d2/map.js was also regenerated — it's back to cities: [] with no cityScale. So both d2 and wo are now build-script output; hand-edits don't survive. Let me read exactly how MapScreen renders labels + the journey path (sizing, transforms) so I can plan durable changes in the generator.

  Read 1 file

Both d2 and wo were regenerated from build-world.py, so durable changes must go in the generator (matching how the France cities are produced). Key rendering facts confirmed:
- neighborLabels (MapScreen:267) render in raw viewBox coords with per-label fontSize={c.size} — I control sizes.
- Journey path (MapScreen:330) is a polyline through non-preview cities sorted by step, strokeWidth=1.5 in viewBox units (no vectorEffect), so it'd render ~3.5× thick on d2's 200-wide frame.

Let me verify what survived in MapScreen and check the generator's structure and de's step/icon values.

  Ran 1 shell command

MapScreen's cityScale mechanism survived (lines 203, 360-361). d2 reverted to cities: []. Now I have what I need on the rendering side. Let me read the generator's output template precisely to know where to inject the German cities, labels, and cityScale.

  Read 1 file

Let me confirm two more things before planning: whether my content.js re-exports survived the regenerations (needed so route cities aren't dead links), and the exact journey-path polyline styling.

  Ran 1 shell command
                                                                                                                                            I now have the complete picture. Let me write the plan.
                                                                                                                                          Summary of findings:
- Both d2/wo are regenerated by build-world.py; durable changes must go in the generator. MapScreen's cityScale mechanism and both        content.js re-exports survived.
- de has three map features d2 lacks: numbered route cities (step + connecting polyline), neighborLabels (surrounding country/sea names), and links (a Paris→France journey jump — out of scope u
- The generator already produces route-less preview dots for France via a bbox→lon/lat affine (fr_xy); I'll mirror it for Germany with step/icon.
