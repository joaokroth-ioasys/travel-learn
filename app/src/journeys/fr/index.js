import content from './content';
import map from './map';
import labels from './labels';

// The French journey pack: everything that is data, in one place.
export default { id: 'fr', content, map, labels };
