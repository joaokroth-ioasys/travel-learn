import content from './content';
import map from './map';
import labels from './labels';

// The Japan journey pack: everything that is data, in one place.
export default { id: 'jp', content, map, labels };
