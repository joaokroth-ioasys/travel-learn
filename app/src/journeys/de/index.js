import content from './content';
import map from './map';
import labels from './labels';
import citylife from './citylife';

// The German journey pack: everything that is data, in one place.
export default { id: 'de', content, map, labels, citylife };
