import content from './content';
import map from './map';
import labels from './labels';
import citylife from './citylife';

// The Chile journey pack: everything that is data, in one place.
export default { id: 'c2', content, map, labels, citylife, citylifeMap: '/city-life-map-chile.png' };
