import content from './content';
import map from './map';
import labels from './labels';

// The "D2" journey pack: the world map framed on Germany (placeholder journey).
export default { id: 'd2', content, map, labels };
